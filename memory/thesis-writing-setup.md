---
name: thesis-writing-setup
description: How/where the thesis (sempro) Word document is written — template, files, method
metadata:
  type: project
---

Thesis is written in Word as a Seminar Proposal (sempro), reusing a friend's finished doc (by **Eriko Syah Putra Friyadi**, a pneumonia/X-ray ResNet50 study) as a **format template**. User is Hamza Pratama (NIM 4611422068), Teknik Informatika UNNES.

**Files (in `C:\Users\Pratama\Downloads\smpro\`):**
- `sempro-hamza-1.docx` — main working doc (open in Word). Page 1 cover already edited to Hamza's [[thesis-title-final]]. BAB 1 & BAB 2 still hold Eriko's pneumonia content (not yet rewritten).
- `sempro-hamza-BAB3.docx` — copy with **BAB 3 fully rewritten** to Hamza's KD/TrashNet topic (done 2026-06-28).

**Doc style IDs (match exactly when generating):** `Judul1` (BAB), `Judul2`/`Judul3`/`Judul4` (auto-numbered headings via numPr — Judul2 numId=14, Judul3 under 3.1=16 & under 3.7=32, Judul4=31), body = no pStyle (TNR 12 / sz24, jc=both, firstLine 426, line 276), `DaftarParagraf` (bullets, numId 24/25/28), `Keterangan` (captions). Foreign terms italicized per PUEBI.

**Method that works:** no python-docx/lxml/pandoc available — edit `word/document.xml` via **string splice** (locate paragraph bounds by unique `w14:paraId`, NOT by text which can be split across runs), generate fragment XML with `w:` prefix, rewrite the zip. Script: scratchpad `gen_bab3.py`. Verified well-formed + opens clean in Word.

**BAB 3 structure written:** 3.1 (Pendekatan kuantitatif / Jenis eksperimental-komparatif / Prosedur + 5 tahap Judul4) · 3.2 Lokasi&Waktu (UNNES, Python+PyTorch+timm+Albumentations, Kaggle; Sep 2025–Jun 2026) · 3.3 Subjek · 3.4 Data (TrashNet, K6/K5) · 3.5 Pengumpulan · 3.6 Keabsahan · 3.7 (Import, Preprocessing, Splitting 70/15/15, Augmentation, Arsitektur, Knowledge Distillation, Training, Evaluation, Uji Statistik, Skenario Pengujian + Tabel 3.2 R0–R9). Dropped Eriko's Class Weight/Mixup.

**BAB 3 status (as of 2026-06-29): COMPLETE** — prose, tables, Persamaan 3.1 (native OMML, editable), and all 3 figures DONE. Gambar 3.1/3.2 = 2-column "snake" flowcharts, Gambar 3.3 = teacher/assistant/student KD architecture; rendered via matplotlib (no cairosvg/lxml available) and injected as PNGs (word/media/image100-102.png, rId48-50). Bullet lists in 3.7 fixed to use numId=25 (bullet •) — was a numId=24 reuse bug that made Arsitektur list count 4,5,6.

**Numbering.xml note:** numId 24=decimal "1)", 25=bullet •, 27=decimal "1)", 28=lowerLetter "a.". Use **25 for bullets**.

**IMPORTANT — Word re-saves/normalizes on close:** after the user opens+closes the doc, all paragraphs get `w14:paraId` and bare `<w:p>` becomes `<w:p ...>`. So locate paragraph bounds via `w14:paraId` (ET to find the para, then string-locate the id), and use `'<w:p '` (with space) not `'<w:p>'`. Word must be CLOSED before any programmatic edit (see [[feedback-docx-editing]]).

**BAB 1 status (as of 2026-06-29): COMPLETE** — rewritten from Eriko's pneumonia content to KD/TrashNet/WasteNet via the same paraId string-splice method (script scratchpad `gen_bab1.py`, 55 paragraph replacements). Sections: 1.1 Latar Belakang (9 paras: sampah→CNN→TrashNet→model ringan/edge→KD→capacity-gap/TA-KD→BAN→gap→this study) · 1.2 Batasan (5 bullets) · 1.3 Rumusan (3) · 1.4 Tujuan (3) · 1.5 Manfaat Praktis/Teoritis · 1.6 Kebaruan Penelitian (intro + 4 related-work paras + Tabel 1.1 + closing). Method kept each para's pPr/numPr verbatim, swapped only run text; Mendeley citation SDT fields dropped (so BAB1 in-text cites are now PLAIN TEXT author-year, NOT Mendeley fields — user must re-link in Mendeley or leave as plain). Italics via `*...*`→`<w:i/>`. **Only real, well-known method refs used (Thung&Yang 2016, Hinton 2015, Cho&Hariharan 2019, Mirzadeh 2020, Furlanello 2018); NO fabricated third-party empirical papers.** 1.6's related-works + Tabel 1.1 were repurposed to these 4 method papers (Hinton/Cho&Hariharan/Mirzadeh/Furlanello) — if Hamza wants actual waste-classification comparison papers there, he must supply real refs. Tabel 1.1 col "Akurasi" header changed to "Fokus Utama". BAB 1 ends before BAB 2 at paraId 1B930DB7 (empty Judul1 page-break).

**BAB 2 status (as of 2026-06-29): COMPLETE** — rewritten from Eriko's pneumonia content to KD/TrashNet via same paraId string-splice (script scratchpad `gen_bab2.py`; figures `figs2.py`). Replaced region paraId 25724413 ("Tinjauan Pustaka") → 0E365ED7 (empty Judul1 before BAB 3); kept "BAB 2"/"KAJIAN PUSTAKA" headings. **Heading numIds for BAB 2 differ from BAB 3**: Judul2=numId 3 (ind left426 hang426), Judul3=numId 2 (left993 hang567), Judul4=numId 20 (left1276 hang850, +italic pPr rPr). Structure: **2.1 Tinjauan Pustaka** (intro + 6 real prior studies Thung&Yang/Zheng/Hinton/Cho&Hariharan/Mirzadeh/Furlanello + Tabel 2.1 ringkasan + gap para) · **2.2 Landasan Teori** 13 Judul3: Klasifikasi Citra Sampah, Deep Learning, CNN, Transfer Learning, Arsitektur Model (EffNet-B4/Focus-RCNet/WasteNet prose), Knowledge Distillation, Teacher-Assistant KD, Self-Distillation(BAN), Preprocessing, Data Augmentation, Dataset TrashNet, Evaluasi (Judul4: Confusion Matrix/Accuracy/Precision/Recall/F1-Score), Uji Statistik. **7 equations** Persamaan 2.1–2.7 inside borderless numbered tables (2.1 softmax/temperature, 2.2 KD loss, 2.3 normalisasi, 2.4 accuracy, 2.5 precision, 2.6 recall, 2.7 F1), rendered as **native OMML** (proper stacked fractions `m:f` + summation `m:nary`, like BAB3's Persamaan 3.1). OMML works fine — it was NEVER the bug (see below). mr() matches gen_eq (no xml:space on m:t). 2.2 is identical structure to BAB3 3.1. **REAL ROOT CAUSE of "Word mengalami kesalahan mencoba membuka file" (2026-06-29): the inline-image `<a:graphicData uri="...">` was set to the drawingml MAIN namespace (`.../2006/main`) instead of the PICTURE namespace (`.../2006/picture`).** Word then can't resolve the picture frame and REFUSES TO OPEN THE WHOLE FILE — even though XML is well-formed, all image rels resolve, bookmarks balanced, etc. (ET.fromstring + every structural lint PASSED; only a byte-diff of my drawing vs the proven BAB3/inject_figs drawing exposed it). My `gen_bab2.figure()` `%`-format passed `A` (main) where the `uri` slot needed `PIC` (picture). FIX = one char: pass `PIC` for `a:graphicData/@uri`. **NOT an OMML problem** — the golden pre-BAB2 baseline has 68 of Eriko's OMML eqs (incl. fractions) and opens fine; I switched BAB2 eqs to plain-text anyway (harmless, kept). Lesson: when Word refuses to open but XML/structure all validate, byte-diff each NEW drawing/construct against a known-working one; check `a:graphicData/@uri == .../drawingml/2006/picture`. Repair re-spliced region 56D78766(KAJIAN PUSTAKA, kept)→0E365ED7, reusing existing rId51–55/image103–107. **5 figures** Gambar 2.1–2.5 (CNN arch, KD scheme, capacity-gap strategies, augmentation, TrashNet class distribution bar-chart) rendered via matplotlib, injected single-pass as image103–107.png / rId51–55. Integrity verified: well-formed, all r:embed resolve, media present. Single-pass: gen_bab2.py emits image drawings + rels + media in one zip write (no separate inject step). Only REAL refs used (no fabricated empirical papers).

**TODO left for user:** verify carried-over citations (Marhasova 2022, Sugiyono 2015) are in DAFTAR PUSTAKA; ensure all BAB2 method/anchor refs present (see [[thesis-citations]]). DAFTAR ISI / Daftar Gambar / Daftar Tabel still show OLD Eriko entries (plain text / stale fields) — must regenerate at the end. Figure/table captions are plain text, not SEQ fields. **SCOPE = SEMINAR PROPOSAL (sempro) → hanya BAB 1–3** (BAB 4 Hasil & BAB 5 Penutup baru di skripsi penuh, BUKAN sekarang). Semua bab isi (1,2,3) SUDAH SELESAI per 2026-06-29 dan **BAB 2 SUDAH DIKONFIRMASI USER bisa dibuka di Word + rumus OMML tampil benar**. NEXT SESSION (user pindah chat baru) = kerjakan DAFTAR PUSTAKA + DAFTAR ISI (+ Daftar Gambar/Tabel). Untuk DAFTAR ISI/GAMBAR/TABEL: CEK DULU apakah itu TOC field hidup (`<w:fldSimple>`/`w:instrText` TOC) yang tinggal Update Field, atau teks statis Eriko yang harus dibangun ulang via splice. Heading pakai style Judul1–4 asli (mestinya kebaca TOC). DAPUS: rakit dari [[thesis-citations]] (semua ref BAB1+2+3 sudah dilog), alfabetis APA7 hanging-indent, buang ref pneumonia Eriko yg tak terpakai. **DAFTAR PUSTAKA = DONE (2026-06-29)** — see [[thesis-citations]] (26 APA-7 entries spliced in, Mendeley SDT placeholder removed, 2 pneumonia orphans swapped). **DAFTAR ISI = LIVE TOC FIELD** (`TOC \o "1-3" \h \z \u`, paraId 2FA3EE28): BAB 1–3 headings kept Judul1–3 styles so the field regenerates correctly — **user only needs Word → klik kanan di Daftar Isi → Update Field → Update entire table** (atau Ctrl+A lalu F9). Cached entries currently stale (Eriko). Page numbers can't be computed outside Word's layout engine, so do NOT hand-build them. **DAFTAR GAMBAR + DAFTAR TABEL = DONE (2026-06-29, option a)** — script scratchpad `gen_lists.py`. Normalized ALL 12 real captions (style Keterangan) to clean uniform numbering: each = literal "`Gambar {ch}.`" / "`Tabel {ch}.`" + visible `<w:fldSimple w:instr=" SEQ Gambar|Tabel \* ARABIC \s 1 ">` (the `\s 1` resets per Judul1 — confirmed Judul1 outlineLvl=0) + " {title}". This replaced 3 inconsistent prior states: Eriko's BAB1 per-chapter field `SEQ Tabel_1.` (rendered "Tabel 1. 1" w/ space), BAB2 plain "Gambar 2. 1" (space), BAB3 plain "Gambar 3.1". Captions: Tabel 1.1/2.1/3.1/3.2 + Gambar 2.1–2.5/3.1–3.3. The stray Keterangan `(Thung & Yang, 2016)` source-note under Gambar 2.5 was EXCLUDED (no SEQ) so it won't list. Then replaced Eriko's two per-chapter ToF fields (`TOC \c "Tabel 1."` @paraId 7C87838F, `TOC \c "Gambar 2."` @09EA3E19 — each only caught ONE chapter) with single unified `TOC \h \z \c "Tabel"` / `\c "Gambar"` that catch all chapters. New captions written as bare `<w:p>` (no paraId; Word re-adds on save). **User must Update Field in Word** (Ctrl+A, F9) to populate page numbers — fig numbers already cached correctly. Front/back matter now COMPLETE: DAFTAR ISI (live TOC), DAFTAR PUSTAKA (26 APA-7 entries), DAFTAR GAMBAR, DAFTAR TABEL all field-driven → one Ctrl+A/F9 updates everything.

**FIX (2026-06-29, gen_clean_lists.py): rebuilt all 3 list regions to pristine single-field + page-break.** First attempt left Eriko's orphan TOC/ToF cache paragraphs (PAGEREF to deleted bookmarks → "Kesalahan! Bookmark tidak ditentukan" + stale pneumonia entries like "Flowchart Penelitian"/"ResNet50 X-ray Dada"). Root cause: the old TOC/ToF fields are ONE big field whose cached result spans ~50 paragraphs; my per-field-block splice didn't wipe all of them. Fix = for each section, replace everything between the Judul1 heading and the NEXT Judul1 with `[clean TOC field placeholder][page-break para]`. Boundaries: ISI[0CF860BD]→TABEL[5866D85E]→GAMBAR[5F9C4AB0]→sectPr-para[12A2F0E9](PRESERVED, holds the roman→arabic sectPr)→BAB1[1000CC8D]. Judul1 has pageBreakBefore=FALSE so explicit page-break paras are kept. Verified zero Eriko leftovers + sectPr intact + new captions intact. Set updateFields=true in settings.xml so Word auto-prompts update on open. **User must CLOSE WITHOUT SAVING (stale session) then reopen → update entire table.** ✅ CONFIRMED WORKING by user 2026-06-30 (reopened, fields updated perfectly, made minor manual adjustments + saved). Sempro front/back matter fully done & verified in Word. Scripts in scratchpad: gen_bab1.py / gen_bab2.py / gen_bab3.py / gen_eq.py / figs2.py / inject_figs.py.
