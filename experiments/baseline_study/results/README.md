# Baseline Results

Executed Kaggle notebooks are stored under `notebooks/`.

For future analysis, also download structured artifacts from Kaggle into
`runs/<run-id>/` when available:

```text
runs/<run-id>/
|-- config.json
|-- summary.json
|-- history.csv
|-- per_class_metrics.csv
|-- confusion_matrix.csv
`-- model_profile.json
```

The existing notebooks remain valid historical evidence. Extracted CSV and JSON
files will make cross-model comparison faster and less error-prone.
